using System;
using System.Collections.Generic;
using System.Linq;

namespace Judge
{
    class Program
    {
        static void Main(string[] args)
        {
            //Dictionary<string, Dictionary<string, int>> contest = new Dictionary<string, Dictionary<string, int>>();

            //Dictionary<string, int> individual = new Dictionary<string, int>();

            //int points = 0;
            //string name = string.Empty;
            //string checkContest = string.Empty;

            //string[] input = Console.ReadLine()
            //    .Split(" -> ");

            //while (input[0] != "no more time")
            //{
            //    points = int.Parse(input[2]);
            //    name = input[0];
            //    checkContest = input[1];

            //    if (!contest.ContainsKey(checkContest))
            //    {
            //        contest.Add(checkContest, new Dictionary<string, int>
            //        {
            //            {name, points }
            //        });
            //        if (!individual.ContainsKey(name))
            //        {
            //            individual.Add(name, points);
            //        }
            //        else
            //        {
            //            individual[name] += points;
            //        }
            //    }
            //    else
            //    {
            //        if (!contest[checkContest].ContainsKey(name))
            //        {
            //            contest[checkContest].Add(name, points);

            //            if (!individual.ContainsKey(name))
            //            {
            //                individual.Add(name, points);
            //            }
            //            else
            //            {
            //                individual[name] += points;
            //            }
            //        }
            //        else
            //        {
            //            if (contest[checkContest][name] < points)
            //            {
            //                individual[name] -= contest[checkContest][name];
            //                contest[checkContest][name] = points;
            //                individual[name] += contest[checkContest][name];
            //            }
            //        }
            //    }

            //    input = Console.ReadLine()
            //    .Split(" -> ");
            //}
            //int counter;
            //foreach (var kvp in contest)
            //{
            //    Console.WriteLine($"{kvp.Key}: {kvp.Value.Count()} participants");

            //     counter = 1;

            //    foreach (var item in kvp.Value
            //        .OrderByDescending(x => x.Value)
            //        .ThenBy(x => x.Key))
            //    {
            //        Console.WriteLine($"{counter}. {item.Key} <::> {item.Value}");
            //        counter++;
            //    }
            //}
            //Console.WriteLine($"Individual standings:");

            //int counterForUsers = 1;

            //foreach (var kvp in individual
            //    .OrderByDescending(x => x.Value)
            //    .ThenBy(x => x.Key))
            //{
            //    Console.WriteLine($"{counterForUsers}. {kvp.Key} -> {kvp.Value}");
            //    counterForUsers++;
            //}



            Console.WriteLine(" Radi ");
        }
    }
}



